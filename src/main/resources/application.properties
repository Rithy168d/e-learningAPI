spring.application.name=e-leariningApi

#config datasource
#register driver
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}

spring.jpa.hibernate.ddl-auto=update

#config email
spring.mail.username=ritty690@gmail.com
spring.mail.password=udxl fiys khxb yach
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

file-upload.server-path=${FILE_UPLOAD_SERVER_PATH}
file-upload.base-uri=${FILE_UPLOAD_BASE_URI}

# file client request
spring.servlet.multipart.max-request-size=10MB
# file system support
spring.servlet.multipart.max-file-size=5MB

